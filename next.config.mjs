/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        hostname: "yfimotmfo45d2zto.public.blob.vercel-storage.com",
      },
    ],
  },
};

export default nextConfig;
